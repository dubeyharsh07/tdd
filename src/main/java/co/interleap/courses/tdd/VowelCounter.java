package co.interleap.courses.tdd;


public class VowelCounter {

    //private static final Set<String> VOWEL_SET = Stream.of("a", "e", "i" , "o", "u").collect(Collectors.toSet());

    public int count(String text) {

        return text.length();
    }
}
